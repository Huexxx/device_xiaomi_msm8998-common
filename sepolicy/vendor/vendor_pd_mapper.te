allow vendor_pd_mapper kmsg_device:chr_file { getattr };
allow vendor_pd_mapper self:capability { sys_admin };
allow vendor_pd_mapper vfat:dir { read open search };
allow vendor_pd_mapper vfat:file { read open };
